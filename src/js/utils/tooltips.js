import React from 'react';
import { Tooltip } from 'react-bootstrap';

export const tooltip_play = (
 	<Tooltip id="tooltip_play"><strong>Play</strong></Tooltip>
);

export const tooltip_up = (
  <Tooltip id="tooltip_up"><strong>Move Up</strong> the playlist</Tooltip>
);

export const tooltip_down = (
  <Tooltip id="tooltip_down"><strong>Move Down</strong> the playlist</Tooltip>
);

export const tooltip_add = (
    <Tooltip id="tooltip_add"><strong>Add</strong> playlist to queue</Tooltip>
);

export const tooltip_delete = (
  <Tooltip id="tooltip_delete"><strong>Delete</strong> playlist</Tooltip>
);

export const tooltip_edit = (
  <Tooltip id="tooltip_edit"><strong>Edit</strong> playlist name</Tooltip>
);

export const tooltip_favourite = (
    <Tooltip id="tooltip_unfavourite"><strong>Favourite</strong> playlist </Tooltip>
);

export const tooltip_unfavourite = (
    <Tooltip id="tooltip_unfavourite"><strong>Unfavourite</strong> playlist </Tooltip>
);

export const tooltip_queue = (
  <Tooltip id="tooltip_queue"><strong>Queue List</strong></Tooltip>
);

export const tooltip_playlist_add = (
  <Tooltip id="tooltip_playlist_add"><strong>Check Off</strong> then click playlist</Tooltip>
);